///
/// HybridShaderTextComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridShaderTextComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridShaderTextSpecSwift.hpp"
#import "ReactNativeShaderText-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::reactnative::shadertext;
using namespace margelo::nitro::reactnative::shadertext::views;

/**
 * Represents the React Native View holder for the Nitro "ShaderText" HybridView.
 */
@interface HybridShaderTextComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridShaderTextComponent {
  std::shared_ptr<HybridShaderTextSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridShaderTextComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridShaderTextComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridShaderTextSpec> hybridView = ReactNativeShaderText::ReactNativeShaderTextAutolinking::createShaderText();
    _hybridView = std::dynamic_pointer_cast<HybridShaderTextSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  ReactNativeShaderText::HybridShaderTextSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridShaderTextProps const>(props);
  auto& newViewProps = const_cast<HybridShaderTextProps&>(newViewPropsConst);
  ReactNativeShaderText::HybridShaderTextSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // segments: array
  if (newViewProps.segments.isDirty) {
    swiftPart.setSegments(newViewProps.segments.value);
    newViewProps.segments.isDirty = false;
  }
  // animation: optional
  if (newViewProps.animation.isDirty) {
    swiftPart.setAnimation(newViewProps.animation.value);
    newViewProps.animation.isDirty = false;
  }
  // blurOutDuration: number
  if (newViewProps.blurOutDuration.isDirty) {
    swiftPart.setBlurOutDuration(newViewProps.blurOutDuration.value);
    newViewProps.blurOutDuration.isDirty = false;
  }
  // delay: number
  if (newViewProps.delay.isDirty) {
    swiftPart.setDelay(newViewProps.delay.value);
    newViewProps.delay.isDirty = false;
  }
  // duration: number
  if (newViewProps.duration.isDirty) {
    swiftPart.setDuration(newViewProps.duration.value);
    newViewProps.duration.isDirty = false;
  }
  // fontSize: number
  if (newViewProps.fontSize.isDirty) {
    swiftPart.setFontSize(newViewProps.fontSize.value);
    newViewProps.fontSize.isDirty = false;
  }
  // fontWeight: string
  if (newViewProps.fontWeight.isDirty) {
    swiftPart.setFontWeight(newViewProps.fontWeight.value);
    newViewProps.fontWeight.isDirty = false;
  }
  // italic: boolean
  if (newViewProps.italic.isDirty) {
    swiftPart.setItalic(newViewProps.italic.value);
    newViewProps.italic.isDirty = false;
  }
  // textAlign: string
  if (newViewProps.textAlign.isDirty) {
    swiftPart.setTextAlign(newViewProps.textAlign.value);
    newViewProps.textAlign.isDirty = false;
  }
  // textColor: string
  if (newViewProps.textColor.isDirty) {
    swiftPart.setTextColor(newViewProps.textColor.value);
    newViewProps.textColor.isDirty = false;
  }
  // lineHeight: optional
  if (newViewProps.lineHeight.isDirty) {
    swiftPart.setLineHeight(newViewProps.lineHeight.value);
    newViewProps.lineHeight.isDirty = false;
  }
  // numberOfLines: optional
  if (newViewProps.numberOfLines.isDirty) {
    swiftPart.setNumberOfLines(newViewProps.numberOfLines.value);
    newViewProps.numberOfLines.isDirty = false;
  }
  // ellipsizeMode: string
  if (newViewProps.ellipsizeMode.isDirty) {
    swiftPart.setEllipsizeMode(newViewProps.ellipsizeMode.value);
    newViewProps.ellipsizeMode.isDirty = false;
  }
  // allowFontScaling: boolean
  if (newViewProps.allowFontScaling.isDirty) {
    swiftPart.setAllowFontScaling(newViewProps.allowFontScaling.value);
    newViewProps.allowFontScaling.isDirty = false;
  }
  // maxFontSizeMultiplier: optional
  if (newViewProps.maxFontSizeMultiplier.isDirty) {
    swiftPart.setMaxFontSizeMultiplier(newViewProps.maxFontSizeMultiplier.value);
    newViewProps.maxFontSizeMultiplier.isDirty = false;
  }
  // combinedAccessibilityLabel: string
  if (newViewProps.combinedAccessibilityLabel.isDirty) {
    swiftPart.setCombinedAccessibilityLabel(newViewProps.combinedAccessibilityLabel.value);
    newViewProps.combinedAccessibilityLabel.isDirty = false;
  }
  // revealBlurRadius: number
  if (newViewProps.revealBlurRadius.isDirty) {
    swiftPart.setRevealBlurRadius(newViewProps.revealBlurRadius.value);
    newViewProps.revealBlurRadius.isDirty = false;
  }
  // revealDuration: number
  if (newViewProps.revealDuration.isDirty) {
    swiftPart.setRevealDuration(newViewProps.revealDuration.value);
    newViewProps.revealDuration.isDirty = false;
  }
  // onAnimationStart: optional
  if (newViewProps.onAnimationStart.isDirty) {
    swiftPart.setOnAnimationStart(newViewProps.onAnimationStart.value);
    newViewProps.onAnimationStart.isDirty = false;
  }
  // onAnimationEnd: optional
  if (newViewProps.onAnimationEnd.isDirty) {
    swiftPart.setOnAnimationEnd(newViewProps.onAnimationEnd.value);
    newViewProps.onAnimationEnd.isDirty = false;
  }
  // onContentHeightChange: optional
  if (newViewProps.onContentHeightChange.isDirty) {
    swiftPart.setOnContentHeightChange(newViewProps.onContentHeightChange.value);
    newViewProps.onContentHeightChange.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return ReactNativeShaderText::ReactNativeShaderTextAutolinking::isShaderTextRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  ReactNativeShaderText::HybridShaderTextSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  ReactNativeShaderText::HybridShaderTextSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  [super invalidate];
}
#endif

@end
