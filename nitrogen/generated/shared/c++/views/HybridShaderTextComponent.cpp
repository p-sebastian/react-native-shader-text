///
/// HybridShaderTextComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridShaderTextComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::reactnative::shadertext::views {

  extern const char HybridShaderTextComponentName[] = "ShaderText";

  HybridShaderTextProps::HybridShaderTextProps(const react::PropsParserContext& context,
                                               const HybridShaderTextProps& sourceProps,
                                               const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    segments([&]() -> CachedProp<std::vector<ShaderTextSegment>> {
      try {
        const react::RawValue* rawValue = rawProps.at("segments", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.segments;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::vector<ShaderTextSegment>>::fromRawValue(*runtime, value, sourceProps.segments);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.segments: ") + exc.what());
      }
    }()),
    animation([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("animation", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.animation;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.animation);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.animation: ") + exc.what());
      }
    }()),
    blurOutDuration([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("blurOutDuration", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.blurOutDuration;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.blurOutDuration);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.blurOutDuration: ") + exc.what());
      }
    }()),
    delay([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("delay", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.delay;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.delay);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.delay: ") + exc.what());
      }
    }()),
    duration([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("duration", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.duration;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.duration);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.duration: ") + exc.what());
      }
    }()),
    fontSize([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("fontSize", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.fontSize;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.fontSize);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.fontSize: ") + exc.what());
      }
    }()),
    fontWeight([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("fontWeight", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.fontWeight;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.fontWeight);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.fontWeight: ") + exc.what());
      }
    }()),
    italic([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("italic", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.italic;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.italic);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.italic: ") + exc.what());
      }
    }()),
    textAlign([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("textAlign", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.textAlign;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.textAlign);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.textAlign: ") + exc.what());
      }
    }()),
    textColor([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("textColor", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.textColor;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.textColor);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.textColor: ") + exc.what());
      }
    }()),
    lineHeight([&]() -> CachedProp<std::optional<double>> {
      try {
        const react::RawValue* rawValue = rawProps.at("lineHeight", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.lineHeight;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<double>>::fromRawValue(*runtime, value, sourceProps.lineHeight);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.lineHeight: ") + exc.what());
      }
    }()),
    numberOfLines([&]() -> CachedProp<std::optional<double>> {
      try {
        const react::RawValue* rawValue = rawProps.at("numberOfLines", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.numberOfLines;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<double>>::fromRawValue(*runtime, value, sourceProps.numberOfLines);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.numberOfLines: ") + exc.what());
      }
    }()),
    ellipsizeMode([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("ellipsizeMode", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.ellipsizeMode;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.ellipsizeMode);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.ellipsizeMode: ") + exc.what());
      }
    }()),
    allowFontScaling([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("allowFontScaling", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.allowFontScaling;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.allowFontScaling);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.allowFontScaling: ") + exc.what());
      }
    }()),
    maxFontSizeMultiplier([&]() -> CachedProp<std::optional<double>> {
      try {
        const react::RawValue* rawValue = rawProps.at("maxFontSizeMultiplier", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.maxFontSizeMultiplier;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<double>>::fromRawValue(*runtime, value, sourceProps.maxFontSizeMultiplier);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.maxFontSizeMultiplier: ") + exc.what());
      }
    }()),
    combinedAccessibilityLabel([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("combinedAccessibilityLabel", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.combinedAccessibilityLabel;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.combinedAccessibilityLabel);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.combinedAccessibilityLabel: ") + exc.what());
      }
    }()),
    revealBlurRadius([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("revealBlurRadius", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.revealBlurRadius;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.revealBlurRadius);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.revealBlurRadius: ") + exc.what());
      }
    }()),
    revealDuration([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("revealDuration", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.revealDuration;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.revealDuration);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.revealDuration: ") + exc.what());
      }
    }()),
    onAnimationStart([&]() -> CachedProp<std::optional<std::function<void()>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onAnimationStart", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onAnimationStart;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void()>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onAnimationStart);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.onAnimationStart: ") + exc.what());
      }
    }()),
    onAnimationEnd([&]() -> CachedProp<std::optional<std::function<void(const ShaderTextAnimationEndResult& /* result */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onAnimationEnd", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onAnimationEnd;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const ShaderTextAnimationEndResult& /* result */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onAnimationEnd);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.onAnimationEnd: ") + exc.what());
      }
    }()),
    onContentHeightChange([&]() -> CachedProp<std::optional<std::function<void(double /* height */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onContentHeightChange", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onContentHeightChange;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(double /* height */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onContentHeightChange);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.onContentHeightChange: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridShaderTextSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridShaderTextSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ShaderText.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridShaderTextProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("segments"): return true;
      case hashString("animation"): return true;
      case hashString("blurOutDuration"): return true;
      case hashString("delay"): return true;
      case hashString("duration"): return true;
      case hashString("fontSize"): return true;
      case hashString("fontWeight"): return true;
      case hashString("italic"): return true;
      case hashString("textAlign"): return true;
      case hashString("textColor"): return true;
      case hashString("lineHeight"): return true;
      case hashString("numberOfLines"): return true;
      case hashString("ellipsizeMode"): return true;
      case hashString("allowFontScaling"): return true;
      case hashString("maxFontSizeMultiplier"): return true;
      case hashString("combinedAccessibilityLabel"): return true;
      case hashString("revealBlurRadius"): return true;
      case hashString("revealDuration"): return true;
      case hashString("onAnimationStart"): return true;
      case hashString("onAnimationEnd"): return true;
      case hashString("onContentHeightChange"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridShaderTextComponentDescriptor::HybridShaderTextComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridShaderTextComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                      const std::shared_ptr<const react::Props>& props,
                                                                                      react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridShaderTextShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridShaderTextComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridShaderTextShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridShaderTextProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridShaderTextProps>& props = std::const_pointer_cast<HybridShaderTextProps>(constProps);
    HybridShaderTextState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::reactnative::shadertext::views
